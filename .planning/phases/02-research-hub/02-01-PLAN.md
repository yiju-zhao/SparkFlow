---
phase: 02-research-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mcp-server/server.py
  - apps/mcp-server/ui/table.html
  - apps/mcp-server/ui/chart.html
  - apps/mcp-server/requirements.txt
  - apps/agent/requirements.txt
files_deleted:
  - apps/web/components/explore/generative-ui/generative-table.tsx
  - apps/web/components/explore/generative-ui/generative-chart.tsx
  - apps/web/components/explore/generative-ui/index.ts
  - apps/agent/tools/hub_queries.py
autonomous: true
requirements:
  - GENUI-01
  - GENUI-02
  - GENUI-03
  - GENUI-04
user_setup: []
must_haves:
  truths:
    - "MCP server can be started and responds to HTTP requests"
    - "MCP server exposes database query tools via SQLDatabaseToolkit"
    - "MCP server serves HTML templates for table and chart UIs"
    - "Old generative-ui React components are deleted"
    - "Old hub_queries.py predefined tools are deleted"
  artifacts:
    - path: "apps/mcp-server/server.py"
      provides: "MCP server with SQLDatabaseToolkit and UI resources"
      min_lines: 80
    - path: "apps/mcp-server/ui/table.html"
      provides: "HTML template for generative tables"
      contains: "postMessage"
    - path: "apps/mcp-server/ui/chart.html"
      provides: "HTML template for Chart.js visualizations"
      contains: "Chart.js"
    - path: "apps/mcp-server/requirements.txt"
      provides: "Python dependencies for MCP server"
      contains: "mcp"
  key_links:
    - from: "apps/mcp-server/server.py"
      to: "PostgreSQL database"
      via: "DATABASE_URL environment variable"
      pattern: "SQLDatabase\\.from_uri"
    - from: "apps/mcp-server/server.py"
      to: "ui/table.html"
      via: "@mcp.resource('ui://table')"
      pattern: "@mcp\\.resource"
---

<objective>
Delete old React-based generative UI components and create a Python MCP server with SQLDatabaseToolkit for dynamic database queries and HTML templates for generative UI rendering.

Purpose: Replace the old useComponent architecture with MCP Apps - a simpler pattern where the MCP server handles database queries and serves HTML templates that CopilotKit renders in sandboxed iframes.
Output: Functional MCP server that can query the database and serve UI templates.
</objective>

<execution_context>
@/Users/eason/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/eason/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-hub/02-CONTEXT.md
@.planning/phases/02-research-hub/02-RESEARCH.md

<interfaces>
<!-- Database schema from Phase 1 -->

From prisma/schema.prisma:
- Venue: id, name, type, description
- Instance: id, name, year, location, website, venueId
- ConferenceSession: id, title, type, date, startTime, endTime, location, speaker[], topic[], abstract, overview, instanceId

<!-- MCP Apps pattern from RESEARCH.md -->

Tool response format for MCP Apps:
```python
return {
    "content": [{"type": "text", "text": json.dumps(data)}],
    "structuredContent": data,
    "_meta": {"ui": {"resourceUri": "ui://table"}}
}
```

UI resource registration:
```python
@mcp.resource("ui://table")
def table_template() -> str:
    return open("ui/table.html").read()
```

FastMCP server initialization:
```python
mcp = FastMCP("HubMCPServer", stateless_http=True, json_response=True)
mcp.run(transport="streamable-http", port=3108)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old generative-ui components and hub_queries.py</name>
  <files>
    apps/web/components/explore/generative-ui/generative-table.tsx
    apps/web/components/explore/generative-ui/generative-chart.tsx
    apps/web/components/explore/generative-ui/index.ts
    apps/agent/tools/hub_queries.py
  </files>
  <action>Delete the old architecture files that are being replaced by MCP Apps.

Files to delete:
1. `apps/web/components/explore/generative-ui/generative-table.tsx` - React table component
2. `apps/web/components/explore/generative-ui/generative-chart.tsx` - React chart component
3. `apps/web/components/explore/generative-ui/index.ts` - useComponent hooks
4. `apps/agent/tools/hub_queries.py` - Predefined tools (replaced by SQLDatabaseToolkit)

Also remove the import from apps/agent/graphs/hub_agent.py:
- Remove: `from tools.hub_queries import list_instances, list_sessions, list_venues, search_sessions`
- Remove: `BACKEND_TOOLS = [list_venues, list_instances, list_sessions, search_sessions]`

This clears the way for the new MCP Apps architecture.
</action>
  <verify>
    <automated>test ! -f apps/web/components/explore/generative-ui/generative-table.tsx && test ! -f apps/agent/tools/hub_queries.py && echo "Files deleted successfully"</automated>
  </verify>
  <done>All old generative-ui React components and hub_queries.py deleted, hub_agent.py imports removed</done>
</task>

<task type="auto">
  <name>Task 2: Create MCP server with SQLDatabaseToolkit</name>
  <files>
    apps/mcp-server/server.py
    apps/mcp-server/requirements.txt
    apps/agent/requirements.txt
  </files>
  <action>Create the Python MCP server package with LangChain SQLDatabaseToolkit for dynamic database queries.

1. Create `apps/mcp-server/` directory structure:
   ```
   apps/mcp-server/
   ├── server.py
   ├── ui/
   │   ├── table.html  (created in Task 3)
   │   └── chart.html  (created in Task 3)
   └── requirements.txt
   ```

2. Create `apps/mcp-server/requirements.txt`:
   ```
   mcp
   langchain-community
   langchain-openai
   langchain-core
   psycopg[binary]
   python-dotenv
   ```

3. Create `apps/mcp-server/server.py`:
   - Use FastMCP with HTTP transport (stateless_http=True, json_response=True)
   - Initialize SQLDatabaseToolkit with database connection from DATABASE_URL
   - Create a LangChain agent that uses toolkit tools internally
   - Expose a `query_conferences` tool that accepts natural language questions
   - Register `ui://table` and `ui://chart` resources (stub for now, templates in Task 3)
   - Run on port 3108 with streamable-http transport

   Key implementation details:
   - Tool returns structured data + UI reference: `_meta: {"ui": {"resourceUri": "ui://table"}}`
   - Include both `content` (text fallback) and `structuredContent` (for UI)
   - Load DATABASE_URL from environment (same as agent)

4. Update `apps/agent/requirements.txt` to add:
   ```
   # MCP Server dependencies (shared)
   mcp
   langchain-community
   ```

The server should start with: `cd apps/mcp-server && python server.py`
</action>
  <verify>
    <automated>cd apps/mcp-server && python -c "from server import mcp; print('MCP server module loads OK')" 2>&1</automated>
  </verify>
  <done>MCP server directory created with server.py, SQLDatabaseToolkit configured, requirements.txt files updated</done>
</task>

<task type="auto">
  <name>Task 3: Create HTML templates for MCP Apps UI</name>
  <files>
    apps/mcp-server/ui/table.html
    apps/mcp-server/ui/chart.html
  </files>
  <action>Create HTML templates for generative table and chart UIs that will be rendered in sandboxed iframes by CopilotKit.

1. Create `apps/mcp-server/ui/table.html`:
   - Full HTML document with embedded CSS and JavaScript
   - Listen for `postMessage` event to receive `{ columns, rows, title }` data
   - Render a styled table with:
     - Title header
     - Column headers from columns array
     - Data rows from rows array
     - Hover states on rows
     - Responsive width
   - Styling: Match hub design (bordered, rounded, subtle shadows)
   - Handle empty data: show "No data to display"
   - Optional: Click-to-sort columns

2. Create `apps/mcp-server/ui/chart.html`:
   - Full HTML document with Chart.js loaded from CDN
   - Listen for `postMessage` event to receive `{ type, data, labels, title }` data
   - Support chart types: bar, line, pie
   - Use Chart.js via CDN: `https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js`
   - Color palette matching hub theme:
     ```javascript
     colors: ['#00D084', '#3b82f6', '#eab308', '#a855f7', '#ef4444', '#f97316']
     ```
   - Responsive sizing (max-width 600px)
   - Handle pie chart differently (no y-axis scale)

3. Update server.py to serve these templates:
   ```python
   @mcp.resource("ui://table")
   def table_template() -> str:
       return open(os.path.join(os.path.dirname(__file__), "ui/table.html")).read()

   @mcp.resource("ui://chart")
   def chart_template() -> str:
       return open(os.path.join(os.path.dirname(__file__), "ui/chart.html")).read()
   ```

Templates must be self-contained HTML files with no external dependencies except Chart.js CDN for charts.
</action>
  <verify>
    <automated>test -f apps/mcp-server/ui/table.html && test -f apps/mcp-server/ui/chart.html && grep -q "postMessage" apps/mcp-server/ui/table.html && echo "Templates created"</automated>
  </verify>
  <done>HTML templates created for table and chart UIs, registered as ui:// resources in MCP server</done>
</task>

</tasks>

<verification>
- MCP server directory exists with server.py
- SQLDatabaseToolkit initialized with DATABASE_URL
- ui://table and ui://chart resources registered
- HTML templates exist and are valid HTML
- Old generative-ui components deleted
- Old hub_queries.py deleted
- Python imports work without errors
</verification>

<success_criteria>
- MCP server starts without errors on port 3108
- SQLDatabaseToolkit can connect to PostgreSQL database
- HTML templates are served via ui:// resources
- Old architecture files are completely removed
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-hub/02-01-SUMMARY.md`
</output>
