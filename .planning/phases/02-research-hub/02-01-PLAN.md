---
phase: 02-research-hub
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/explore/generative-ui/generative-table.tsx
  - apps/web/components/explore/generative-ui/generative-chart.tsx
  - apps/web/components/explore/generative-ui/index.ts
autonomous: true
requirements:
  - GENUI-02
  - GENUI-03
  - GENUI-05
user_setup: []
must_haves:
  truths:
    - "AI assistant can display a table with session/conference data in the chat"
    - "AI assistant can display a chart (bar, line, or pie) in the chat"
    - "Generated components have consistent styling with the hub"
  artifacts:
    - path: "apps/web/components/explore/generative-ui/generative-table.tsx"
      provides: "Sortable, paginated table with row click navigation"
      min_lines: 60
    - path: "apps/web/components/explore/generative-ui/generative-chart.tsx"
      provides: "Bar, line, and pie chart rendering"
      min_lines: 80
    - path: "apps/web/components/explore/generative-ui/index.ts"
      provides: "useGenerativeComponents hook for CopilotKit registration"
      exports: ["useGenerativeComponents", "GenerativeTable", "GenerativeChart"]
  key_links:
    - from: "apps/web/components/explore/generative-ui/index.ts"
      to: "@copilotkit/react-core/v2"
      via: "useComponent hook"
      pattern: "useComponent\\(\\{"
---

<objective>
Create generative UI components (table and chart) that CopilotKit can render inline in chat responses.

Purpose: Enable the AI assistant to display structured data visually instead of just text.
Output: Reusable generative components registered with CopilotKit's useComponent hook.
</objective>

<execution_context>
@/Users/eason/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eason/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-research-hub/02-CONTEXT.md
@.planning/phases/02-research-hub/02-RESEARCH.md

<interfaces>
<!-- Key types and patterns from existing codebase -->

From apps/web/components/explore/hub/topics-chart.tsx (ECharts pattern):
```typescript
interface TopicsChartProps {
  data: { topic: string; count: number }[];
}
// Uses useECharts hook, theme-aware colors
```

From apps/web/components/explore/hub/year-trend-chart.tsx:
```typescript
interface YearTrendChartProps {
  data: { year: number; publications: number }[];
}
// ECharts bar chart with borderRadius, theme colors
```

From apps/web/lib/copilotkit-provider.tsx:
```typescript
<CopilotKit runtimeUrl="/api/copilotkit" agent="hub">
```

From apps/agent/tools/hub_queries.py (data structure):
- Sessions have: id, title, type, date, speaker[], topic[], venue_name, year
- Venues have: id, name, type, description, instance_count
- Instances have: id, name, year, venue_name, location, session_count
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GenerativeTable component</name>
  <files>apps/web/components/explore/generative-ui/generative-table.tsx</files>
  <behavior>
    - Test 1: Renders table with columns and rows from props
    - Test 2: Sorts rows when column header clicked
    - Test 3: Paginates when pageSize exceeded
    - Test 4: Calls router.push when row clicked and rowLinkPrefix provided
  </behavior>
  <action>Create a sortable, paginated table component for generative UI.

Implementation requirements:
1. Define Zod schema `GenerativeTableProps` with:
   - title: string (table title)
   - columns: array of { key, label, type? }
   - rows: array of record objects
   - rowLinkPrefix?: string (URL prefix for row navigation)
   - pageSize?: number (default 10)

2. Use Shadcn/UI Table components (Table, TableHeader, TableBody, TableRow, TableCell, TableHead)

3. Features:
   - Sort by any column (click header to toggle asc/desc)
   - Pagination with "Previous"/"Next" buttons
   - Row click navigation using useRouter().push()
   - Show result count in header
   - External link icon for navigable rows

4. Styling:
   - Match existing hub card styling (bg-card, rounded-lg)
   - Consistent with conference list visual style
   - Theme-aware colors via Tailwind

5. Handle edge cases:
   - Empty rows: show "No data to display"
   - No rowLinkPrefix: no click handler or external icon

Use z.infer<typeof GenerativeTableProps> for component props type.
</action>
  <verify>
    <automated>npm run build -- --filter=generative-table 2>&1 | grep -E "(error|warning|Successfully)" || echo "Build check complete"</automated>
  </verify>
  <done>GenerativeTable component exists with Zod schema, supports sorting/pagination/navigation, matches hub styling</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GenerativeChart component</name>
  <files>apps/web/components/explore/generative-ui/generative-chart.tsx</files>
  <behavior>
    - Test 1: Renders bar chart with data prop
    - Test 2: Renders line chart when chartType="line"
    - Test 3: Renders pie chart when chartType="pie"
    - Test 4: Shows empty state when data array is empty
  </behavior>
  <action>Create a multi-type chart component for generative UI.

Implementation requirements:
1. Define Zod schema `GenerativeChartProps` with:
   - title: string (chart title)
   - chartType: enum ["bar", "line", "pie"]
   - data: array of { label: string, value: number }

2. Use existing useECharts hook pattern from apps/web/hooks/use-echarts.ts (NOT Recharts - match existing codebase)

3. Chart configurations:
   - Bar: vertical layout, yAxis shows labels, rounded corners, theme colors
   - Line: xAxis labels, smooth line, dots on data points, theme colors
   - Pie: donut style with labels, multi-color segments from palette

4. Use the same color palette as existing hub charts:
   - Primary: "#00D084" (green)
   - Fallback palette: ["#00D084", "#3b82f6", "#eab308", "#a855f7", "#ef4444", "#f97316"]

5. Styling:
   - Container: bg-card, rounded-lg, p-4
   - Responsive height: 200px
   - Theme-aware via useTheme()

6. Handle edge cases:
   - Empty data: show "No data to display" message

Use z.infer<typeof GenerativeChartProps> for component props type.
</action>
  <verify>
    <automated>npm run build -- --filter=generative-chart 2>&1 | grep -E "(error|warning|Successfully)" || echo "Build check complete"</automated>
  </verify>
  <done>GenerativeChart component exists with Zod schema, supports bar/line/pie types, uses existing ECharts pattern, matches hub styling</done>
</task>

<task type="auto">
  <name>Task 3: Create useGenerativeComponents hook</name>
  <files>apps/web/components/explore/generative-ui/index.ts</files>
  <action>Create the registration hook that wires components to CopilotKit.

Implementation:
1. Import useComponent from "@copilotkit/react-core/v2" (per RESEARCH.md recommendation)

2. Create useGenerativeComponents() hook that registers:
   - "showTable" component with GenerativeTableProps schema
     - description: "Display a table with session or conference data"
   - "showChart" component with GenerativeChartProps schema
     - description: "Display a chart (bar, line, or pie) with data from the research hub"

3. Export both components and the hook:
   - export { GenerativeTable, GenerativeTableProps }
   - export { GenerativeChart, GenerativeChartProps }
   - export { useGenerativeComponents }

4. Add "use client" directive at top

This hook will be called inside the ResearchAssistantPanel component.
</action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>useGenerativeComponents hook exists, registers showTable and showChart with CopilotKit, exports all components</done>
</task>

</tasks>

<verification>
- All three files exist in apps/web/components/explore/generative-ui/
- TypeScript compiles without errors
- Components follow existing hub styling patterns
- Zod schemas properly defined with .describe() for AI understanding
</verification>

<success_criteria>
- GenerativeTable renders with sorting, pagination, row navigation
- GenerativeChart renders bar, line, and pie charts
- useGenerativeComponents hook registers both with CopilotKit
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-research-hub/02-01-SUMMARY.md`
</output>
